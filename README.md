

## Knowledge Index

Build a portable structural map of the active workspace:

```text
index build
index status
index find SessionMemory
index classes
index functions
index todos
index imports
```

The index is stored in `.orion/knowledge-index.json` and follows the active workspace.
